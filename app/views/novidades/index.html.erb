<div id="ladodireito">
  <h2>Novidades</h2>
  <br>  

  <table class="noticiasrelatorio">
    <tr>
      <th>Título</th>
      <th>Resumo</th>
      <th></th>
      <th></th>
    </tr>

  <% @novidades.each do |novidade| %>
    <tr>
      <td><%= novidade.titulo %></td>
      <td><%= novidade.resumo %></td>
      <td><%= link_to '', edit_novidade_path(novidade), :class=> "editar"%></td>
      <td><%= link_to '', novidade, confirm: 'Deseja realmente apagar?', method: :delete, :class=>"deletar" %></td>
    </tr>
  <% end %>
  </table>

</div>
