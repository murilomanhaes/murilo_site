<div id="ladodireito">
  <h2>Parceiros</h2>
  <br>  

  <table class="noticiasrelatorio">
    <tr>
      <th>Nome</th>
      <th>Endereço</th>
      <th></th>
      <th></th>
    </tr>

  <% @parceiros.each do |parceiro| %>
    <tr>
      <td><%= parceiro.nome %></td>
      <td><%= parceiro.endereco %></td>
      <td><%= link_to '', edit_parceiro_path(parceiro), :class=> "editar"%></td>
      <td><%= link_to '', parceiro, confirm: 'Deseja realmente apagar?', method: :delete, :class=>"deletar" %></td>
    </tr>
  <% end %>
  </table>

</div>
